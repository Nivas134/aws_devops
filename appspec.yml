version: 0.0
os: linux

hooks:
  ApplicationStop:
    - location: scripts/stop_server.sh
      timeout: 300
      runas: root
  AferInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
      runas: root
